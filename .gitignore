# ============================================================
#  ⚠️ 这个文件是"防泄漏安全网"，改动时请务必谨慎
#  原则：**真实凭证、运行数据、个人标识一律排除**
# ============================================================

# ---- 🔴 private/ 整个目录：本地私有，永不提交 ----
# 含服务器 IP、API Key、AppSecret、主人 openid、日志页密码、审计特征清单
private/

# ---- 🔴 真实凭证（任何位置的同名文件，双保险）----
关键信息速查.md
.env
.env.*
!.env.example

# ---- 🔴 脱敏审计用的真实特征清单 ----
# 里面是"真实密钥/邮箱/openid/昵称"的正则，本身不能上传
.sanitize-patterns.local

# ---- 🔴 交接文档（历史文件名，已由 docs/项目文档.md 取代）----
#
#  ⚠️ 注意 docs/项目文档.md 是**脱敏后**的版本（真实值一律占位符），
#     和 归档/ 下的运维文档同级，允许提交。
#     如果你希望它只留在本地、不进 GitHub，在下面加一行 `docs/` 即可。
项目交接文档.md

# ---- 🔴 配置备份（内含历史密钥）----
config.js.bak-*
*.bak-*
*.bak

# ---- 🔴 运行数据（含真实聊天记录与人群标识）----
server/data/
data/
*.jsonl
budget.json

# ---- 🔴 SSH 相关 ----
*.pem
*.key
id_rsa*
id_ed25519*
known_hosts

# ---- Node ----
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db
desktop.ini

# ---- 临时产物 ----
*.log
*.tmp
tmp/
temp/
